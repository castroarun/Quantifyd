# Python
__pycache__/
*.py[cod]
*$py.class

# Heavy backtest artifacts (regeneratable from scripts; commit only ranked summary)
research/29_short_options_signal_sweep/results/phase1_signals.csv
research/29_short_options_signal_sweep/results/phase1_trajectories.csv
research/29_short_options_signal_sweep/results/phase2_exits.csv

# Throwaway VPS-probe scripts
/tmp_*.py
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
ENV/
env/
.venv/

# Jupyter Notebooks
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data files (may be large)
data/*.csv
data/*.parquet
data/*.pkl
data/*.h5
data/fundamentals_cache/
!data/.gitkeep
!data/nifty500_list.csv

# Backtest data: ignore ALL binary artifacts. Live trading state (SQLite DBs)
# must NEVER be tracked — a `git pull` that touches a tracked DB will
# overwrite live runtime data. 2026-04-24 incident: git reset wiped
# orb_positions table mid-session.
# Large files and any snapshot-worthy artifacts go to GitHub Releases via
# scripts/backup_to_github_release.sh.
backtest_data/*
!backtest_data/.gitkeep

# Static config / result JSONs (backtest artifacts, instrument tokens)
!backtest_data/*.json

# Never commit secrets, even with the allow-lists above
backtest_data/access_token.json

# Runtime state written each time a job fires — daily-changing, never
# meaningful in git history. The validator and pre-market brief publish
# their latest report here so the API endpoints can serve it.
backtest_data/validator_latest_*.json
backtest_data/premarket_brief_latest.json

# Big data files that must never hit git (use GitHub Releases instead)
backtest_data/market_data.db
backtest_data/options_data.db
backtest_data/*.pkl
backtest_data/fundamentals_cache/

# SQLite journals / WAL files
*.db-journal
*.db-wal
*.db-shm

# Flask session files
flask_session/

# Environment variables
.env
.env.local
.env*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Local settings
.claude/settings.local.json

# Test/Coverage
.coverage
htmlcov/
.pytest_cache/
.tox/

# Logs
*.log
logs/

# Secrets (never commit)
*.pem
*.key
credentials.json
secrets.json

.vercel

# Sweep logs (verbose output)
*_sweep_log.txt
*_log.txt
scc/.next/
scc/node_modules/

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Local-only helper scripts (may contain credentials)
scripts/_vps_helper.py

# Scheduled tasks personal state
.claude/scheduled_tasks.lock
scc/tsconfig.tsbuildinfo
research/29_short_options_signal_sweep/results/pathc_stocks_signals.csv
research/30_volume_breakout/results/volbreakout_signals.csv
research/30b_volume_breakout_expanded/results/volbreakout_signals.csv
research/30b_volume_breakout_expanded/results/volbreakout_signals.csv
research/30b_volume_breakout_expanded/results/run.log
